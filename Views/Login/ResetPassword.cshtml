@{
    ViewData["Title"] = "Restablecer Contraseña";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow border-0">
                <div class="card-body p-4">
                    <h4 class="text-center mb-3"><i class="bi bi-key"></i> Restablecer contraseña</h4>

                    @if (ViewBag.Error != null)
                    {
                            <div class="alert alert-danger">@ViewBag.Error</div>
                    }
                    @if (ViewBag.Message != null)
                    {
                            <div class="alert alert-success">@ViewBag.Message</div>
                    }

                    <form method="post" asp-action="ResetPassword">
                        <input type="hidden" name="token" value="@ViewBag.Token" />

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Nueva contraseña</label>
                            <input type="password" name="nueva" class="form-control" required placeholder="********" />
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Confirmar contraseña</label>
                            <input type="password" name="confirmar" class="form-control" required placeholder="********" />
                        </div>

                        <button type="submit" class="btn btn-primary w-100 text-white fw-semibold">Cambiar contraseña</button>

                        <div class="text-center mt-3">
                            <a asp-action="Index" class="btn btn-danger text-white fw-semibold">Volver</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
